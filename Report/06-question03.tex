\section{Wie spawnt ein Gegner und wie wird er angezeigt?}

\subsection{Methodik}

(Graph noch verändern!!!!!!!!!!)
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Bilder/06-question03_01.png}
    \caption{Übersichtsgraph}
    \label{fig:enter-label}
\end{figure}

Der vorangegangene Graph stellt eine Übersicht dar, wie ein Monster spawnt, angefangen auf einer Metalogik und endend 
auf einer Sprite Ebene. \\
Es fängt in der 29. Rombank im Bereich: 0x001C4E0D - 0x001C4F14 an. Dort stehen folgende Daten:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Bilder/06-question03_02.png}
    \caption{radare2 Ausgabe}
    \label{fig:enter-label}
\end{figure}

Wie am vorangegangenen Graphen zu sehen fängt es im Bereich 0xC240 - 0xC3e0 an. 
Dort stehen die Logikwerte der Objekte, wie zum Beispiel die Position vom Gegner. 
Beim Betreten eines Raumes wird folgendende if-Bedingung überprüft:

if 0xceb0 == 0, dann muss kein Gegner gespawnt werden
falls nein dann schreibe unter anderem die Werte in 0xC240 - 0xC260 rein.
Die Anzahl der zu spawnenden Monster wird an der Adresse 0xCEB0 gespeichert. Und diese Daten kommen 
ursprünglich von 0x1c4e0d: 

Und damit der Algorithmus weiß an welcher Stelle er einlesen muss gibt es für jeden Raum eine weitere Tabelle:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Bilder/06-question03_03.png}
    \caption{speicher}
    \label{fig:enter-label}
\end{figure}

Jetzt ist die Blackbox und die logikwerte des Maulwurfs werden übersetzt in Tiles, sodass wir uns jetzt auf einer Tileebene befinden.
Ein Metaobjekt besteht aus 4 Objektsprites. Und jeder Objektsprite besteht aus 4 bytes:
nulltes Byte: Y Position
erstes Byte: X Position
zweites Byte: Tile number
drittes Byte: Attribute

Mithilfe des OAM DMG Transfer (wenn man an Adresse 0xff46 schreibt) werden die Daten in den OAM Bereich kopiert. Die Objekte werden nun angezeigt.

\subsection{Ergebnis}

Wir können jetzt die Tabelle der zu spawnenden Monster verändern und somit in jedem Raum bestimmen wo und wie viele Monster spawnen.
Es gibt es Limit von insgesamt 16 Monstern, da dafür der vorgegebene Speicher nicht ausreicht.
In unserem Roomeditor können wir jetzt auch Monster platzieren.
